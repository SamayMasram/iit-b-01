import arithmetic
import geometry as geo
from geometry import calc_rect_area
from geometry import calc_rect_peri

def calc_rect_area(len, width):
    print("rect area: ", len * width)
def calc_rect_peri(len, width):
    print("rect perimeter: ", 2 * (len + width))
    